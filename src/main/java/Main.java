import java.io.IOException;

import exceptions.InvalidActionException;

import exceptions.ListIsEmptyException;


public class Main {
    public static void main(String[] args) throws IOException, ListIsEmptyException, InterruptedException, InvalidActionException {
        /*
        ArrayUnorderedList<ILocal> list = ShortestPath.shortestPath(graphMap, portal1, portal3);
        for(ILocal local : list){
            System.out.println(local);
        }
         */
        Menu menu;
        Menu.startGame();
    }

}